apiVersion: v1
kind: Service
metadata:
  name: rag-aerospace
  namespace: rag-aerospace
spec:
  type: ClusterIP
  selector: { app: rag-aerospace }
  ports:
    - name: http
      port: 80
      targetPort: 8000
